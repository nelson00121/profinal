package com.example.applogin;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;
import android.widget.Toast;

import org.w3c.dom.Text;

public class RecuContra extends AppCompatActivity {
 //   EditText usariocabio;
   // Button reestablecer;

    //daUsuarios dao;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
     /*   setContentView(R.layout.activity_recu_contra);
        reestablecer = (Button) findViewById(R.id.btncomprubausuairo);
        usariocabio = (EditText) findViewById(R.id.editUsuarioRestacontraseña);
        dao = new daUsuarios(this);

        reestablecer.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Usuario c = new Usuario();
            dao.buscar(c.Usuario.toString());
            }
        });*/

    }
}